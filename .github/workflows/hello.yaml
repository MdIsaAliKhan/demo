name: hello

on:
  push:
    branches:
      - main
    
  workflows_dispatch:

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Print hello message
        run: echo "Hello, world!"
          